# include<stdio.h>
# include<math.h>

    int main(){
    int side;
    printf("enter the value of side of square\n");
    scanf("%d", &side);
    printf("the value of area is %f\n", pow(side,2));//pow is a function that rasies power to any integer 


    return 0;
}