# include<stdio.h>

    int main(){
    int principal=100, rate=9, years=5;
    int simpleintrest = (principal * rate * years)/100;
    printf("the value of simple intrest is %d", simpleintrest);
    return 0;
}