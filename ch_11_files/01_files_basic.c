# include<stdio.h>

    int main(){
        FILE *ptr;
        ptr = fopen("sample.txt", "r"); //--> for reaading the file
        //ptr = fopen("sample.txt", "w"); //--> for writing in the file 
        int num;

    return 0;
}